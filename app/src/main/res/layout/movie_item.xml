<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="movie"
            type="com.mvvm.data.repo.model.Result" />

        <variable
            name="viewmodel"
            type="com.mvvm.tmdb.ui.BaseViewModel" />
    </data>

    <androidx.cardview.widget.CardView
        style="@style/movie_card_view"
        android:layout_width="@dimen/card_width_height"
        android:layout_height="@dimen/card_width_height">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingTop="@dimen/list_item_padding"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:paddingBottom="@dimen/list_item_padding">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                app:setImage="@{movie.poster_path!= null ? movie.poster_path : @string/def_image}"
                tools:src="@android:drawable/ic_menu_search" />

            <TextView
                android:id="@+id/title_text"
                style="@style/movie_titles"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:text="@{movie.title}"
                tools:text="Movies Title" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>
